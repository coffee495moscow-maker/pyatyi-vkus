# Исходный прототип

`pyatyi_vkus_site_ready_b2e2d8.zip` — статический HTML/CSS/JS прототип,
с которого стартовал редизайн (см. корневой `README.md`). Оставлен для
истории: дизайн-токены и весь текстовый контент были перенесены в
`content/*.ts` и `app/globals.css` дословно. Не используется в сборке.
